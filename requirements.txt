# Core NLP — entity extraction (3-tier: GLiNER → BioClinicalBERT → rule-based)
gliner>=0.2.0
transformers>=4.35.0
torch>=2.0.0

# Pipeline utilities
pyyaml>=6.0
numpy>=1.24.0
scikit-learn>=1.3.0

# API (also in requirements-api.txt — kept here for single-file install)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Optional — data analysis
pandas>=2.0.0
